language: python
python:
  - "3.7"
# command to run tests
install:
  - pip install -r requirements.txt
script:
  - python -m  unittest discover -s  Test

branches:
  only:
    - main
    - development